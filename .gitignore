# dependências
node_modules/

# saída de build (gerada pelo host no deploy)
dist/

# variáveis de ambiente locais
.env
.env.local
.env.*.local

# dados persistidos localmente pelo servidor de dev
works-db.json
signals-db.json

# logs
*.log
