
import React from 'react';

/**
 * COMPONENTE DEPRECIADO
 * Substituído pela nova arquitetura de páginas (PageMateria, PageSinais, etc).
 */

const FieldView: React.FC = () => {
  return null;
};

export default FieldView;
